#Application Configuration
server:
  port: 8092
  error:
    include-message: always
    include-binding-errors: always

spring:
  data:
    mongodb:
      authentication-database: admin
      username: MongoDB
      password:
      database: photoDB
      port: 27017
      host: localhost
      repositories:
        enable: true
      uri: mongodb+srv://userMDB:passMDB@cluster0.onefe.mongodb.net/photoDB